name: Pull Request Merged

on:
  pull_request:
    types: [closed, opened, edited]

jobs:
  pr_merged:
    runs-on: ubuntu-latest

    steps:
      - name: "log"
        run:
          echo "::debug::${{ github.head_ref }}"
